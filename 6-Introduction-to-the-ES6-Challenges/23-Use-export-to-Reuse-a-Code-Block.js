/* Learned:
  - To `import` anything, that thing must be `exported` from somewhere else
   the `export` keyword, followed by an expression, exports that expression.
*/

"use strict";
export const foo = "bar";
export const bar = "foo";