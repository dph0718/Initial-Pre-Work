/* Learned:
  - `for` loops take 3 parameters: ([define a variable where the loop starts], [the condition in which the loop will still run]; [incrementer to use after loop has run])
*/


// Example
var ourArray = [];

for (var i = 0; i < 5; i++) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.

for (var j = 1; j <= 5; j++) {
  myArray.push(j);
}