/* Learned:
  - `==` detects equality, whether the values are of the same type or not. 
*/

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);