/* Learned:
  - Equality operators return a boolean `true` or `false`, so 
   a lengthy 'if/else' statement isn't  necessary. 
*/

function isLess(a, b) {
  // Fix this code
  // if (a < b) {
  //   return true;
  // } else {
  //   return false;
  // }

  return a < b;
}

// Change these values to test
isLess(10, 15);