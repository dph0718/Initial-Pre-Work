/* Learned:
  - pre-defined mathematical operations can be done by using `Math`
  `Math.random()` will return a floating point number >= 0 && < 1 
*/

function randomFraction() {

  // Only change code below this line.
  return Math.random();

  // Only change code above this line.
}