/* Learned: 
    - JS datatypes are 
        - boolean (true, false)
        - string ("a set of characters")
        - number (4, 2, 992, Infinity, NaN, etc.)
        - undefined (undefined)
        - null (nothin)
        - symbol  (?)
        - object

Variables store data and are declared by typing 'var' followed by the name you're giving to the variable.
*/

// Example
var ourName;

// Declare myName below this line
 var myName;