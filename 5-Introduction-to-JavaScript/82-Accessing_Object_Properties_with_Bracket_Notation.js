/* Learned:
  - Bracket notation, like with arrays, can grab the value of the property.
    the property name has to be in quotes within the square brackets.
*/

// Setup
var testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Only change code below this line

var entreeValue = testObj["an entree"];   // Change this line
var drinkValue = testObj["the drink"];    // Change this line