/* Learned:
  - the `\` can escape another `\`
  - `\n `means a newline
  - `\r` means a carriage return
  - `\t` means a tab
  - `\b\` means a backspace
  - `\f` means form feed
  - None of those need spaces around them to work.
*/

var myStr = "FirstLine\n\t\\SecondLine\nThirdLine"; // Change this line

